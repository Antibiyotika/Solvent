<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Theme-agnostic shared styles: everything here uses DynamicResource,
         so it works unmodified under both Colors.xaml and ColorsLight.xaml
         and is never touched by ThemeService's dictionary swap (only
         dictionaries whose Source contains "Colors" are swapped). -->

    <!-- Small rounded status pill: a colored dot + label, used for
         "Administrator", severity/result badges, live status, etc. -->
    <Style x:Key="SolventBadge" TargetType="Border">
        <Setter Property="CornerRadius" Value="999" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="Background" Value="{DynamicResource SolventAccentSoftBrush}" />
    </Style>

    <Style x:Key="SolventBadgeText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource SolventAccentBrush}" />
    </Style>

    <!-- Thin 1px divider for separating header/content blocks without a full card border. -->
    <Style x:Key="SolventDivider" TargetType="Border">
        <Setter Property="Height" Value="1" />
        <Setter Property="Background" Value="{DynamicResource SolventBorderBrush}" />
        <Setter Property="Margin" Value="0,16" />
    </Style>

    <!-- Large numeric value used inside stat cards. -->
    <Style x:Key="SolventStatValue" TargetType="TextBlock">
        <Setter Property="FontSize" Value="21" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource SolventTextPrimaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="SolventStatLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource SolventTextSecondaryBrush}" />
    </Style>

    <Style x:Key="SolventStatHint" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource SolventTextTertiaryBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!-- Thin progress bar variant used under stat card values. -->
    <Style x:Key="SolventThinProgressBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="5" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Foreground" Value="{DynamicResource SolventAccentBrush}" />
        <Setter Property="Background" Value="{DynamicResource SolventBorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <!-- Sidebar/footer "Run All" style glow — a soft pulsing accent ring
         behind an icon, reused by the dashboard hero CTA. -->
    <Style x:Key="SolventGlowDot" TargetType="Ellipse">
        <Setter Property="Width" Value="8" />
        <Setter Property="Height" Value="8" />
        <Setter Property="Fill" Value="{DynamicResource SolventAccentBrush}" />
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
